<Window x:Class="SeatRandomizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="学生座位随机排序" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnRandomize" Content="随机排序" Width="100" Height="30" Click="BtnRandomize_Click" Margin="0,0,10,0"/>
            <Button x:Name="btnEditConfig" Content="编辑配置" Width="100" Height="30" Click="BtnEditConfig_Click" Margin="0,0,10,0"/>
            <TextBlock Text="随机种子:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="txtSeed" Width="150" Height="30" Margin="0,0,10,0" VerticalAlignment="Center" 
                     ToolTip="输入数字和/或字母作为随机种子，相同种子产生相同排序"/>
            <Button x:Name="btnExport" Content="导出当前布局" Width="120" Height="30" Click="BtnExport_Click"/>
        </StackPanel>

        <!-- 讲台 -->
        <Border x:Name="lecternBorder" Grid.Row="1" Height="50" Background="#FFD700" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10">
            <TextBlock Text="讲台" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <!-- 座位网格容器 -->
        <Grid x:Name="seatsContainer" Grid.Row="2">
            <ScrollViewer x:Name="seatScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <Grid x:Name="seatGridContainer"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>